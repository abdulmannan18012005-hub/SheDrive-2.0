{
  "name": "shedrive-2.0",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "mobile",
    "server",
    "admin-portal"
  ],
  "scripts": {
    "lint": "npm run lint --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "devDependencies": {
    "prettier": "^3.0.0",
    "typescript": "^5.0.0"
  }
}
